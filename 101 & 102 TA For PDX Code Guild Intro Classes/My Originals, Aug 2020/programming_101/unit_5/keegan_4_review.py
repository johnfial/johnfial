blank_list = []
print(blank_list)
print(type(blank_list))

# common to create a blank list in order to dynamically add items later

# index in a variablo
# what was the fruits to_change = 1 thing for??

# if you DO KNOW, you can use the for x in range,
# if not, use len(sequence) function

list = len(listname)
then for index in range(len(fruits)):
