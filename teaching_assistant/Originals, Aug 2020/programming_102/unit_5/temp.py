# You'll have to explicitly print it
# You could create a mini REPL to check that you're not getting the default value
# while user_input = default_value from .get() => loop
# then you could print a little message and ask them to repeat the process

# confusing that you WRITE {} for dictionaries, but USE them with []

# lookup what 'subscript' means and -able
# print(list(fruit_prices_dictionary.keys())) # will give a list of the keys!!

# TERM 'GLOBAL SCOPE' is the LEFTMOST indented position --- and will be run when anything is imported and it's on the left

print(locals)
print(__name__)