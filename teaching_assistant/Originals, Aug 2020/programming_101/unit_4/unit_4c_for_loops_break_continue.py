animals = ['jag', 'cat', 'dog', 'eleph', 'mono']


# if possible, keep variable name the SINGULAR version of the LIST name
for animal in animals:
    # loops this
    message = f'current animal: {animal}'
    print(message)
    # ends at last item
print('loop ended')

# STRINGS ARE ALSO SEQUENCES ... so it's a list, sort of, with an index at each character

# int
# string
# bool
# list will convert to a list
# print(list(range(10)))

# loop controls now,
# keywords, continue, break

for x in range(10):
    if
    message = f'number: {x}'

    message = 